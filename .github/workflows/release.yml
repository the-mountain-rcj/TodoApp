name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    env:
      TODOAPP_UPLOAD_RELEASE: ${{ secrets.TODOAPP_UPLOAD_RELEASE }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Inno Setup
        run: choco install innosetup -y --no-progress

      - name: Build installer
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart("v")
          ./build-installer.ps1 -Version $version

      - name: Upload unsigned CI artifacts
        uses: actions/upload-artifact@v4
        with:
          name: todoapp-windows-unsigned
          path: |
            dist/TodoAppSetup.exe
            dist/TodoAppSetup.exe.sha256
            dist/TodoApp.exe

      - name: Upload GitHub Release assets
        if: ${{ env.TODOAPP_UPLOAD_RELEASE == 'true' }}
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/TodoAppSetup.exe
            dist/TodoAppSetup.exe.sha256
            dist/TodoApp.exe
